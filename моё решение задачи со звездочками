#include <stdi.h>
#include <iostream>
using namespace std;
int main ()
{
 int i, j;
 const int n = 10;
 char s[n];
 char a[n];
 cin >> s;
 for ( i = 0; s[i] != '\0'; i++)
  if ( s[i] == '*')
   for ( j = 0; j < n - 1; j++)
    a[j] = s[i + j];
printf ( "%s\n", a);
}
